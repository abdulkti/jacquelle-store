[phases.setup]
nixPkgs = ["php82", "php82Packages.composer", "php82Extensions.pgsql", "php82Extensions.intl", "php82Extensions.mbstring"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader --no-interaction"]

[start]
cmd = "php spark serve --host 0.0.0.0 --port $PORT"
